#!/usr/bin/python3
def divisible_by_2(my_list=[]):
    newlist =my_list[:]
    for I in newlist range(len(new_list)):
        if I % 2 == 0:
            newlist[I] = True
        else:
            newlist[I] = False
    return newlist
