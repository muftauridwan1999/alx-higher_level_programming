#!/usr/bin/python3
def divisible_by_2(my_list=[]):
    if not my_list:
        return my_list
    for I in newlist:
        if I % 2 == 0:
            newlist[I] = True
        else:
            newlist[I] = False
    return newlist
